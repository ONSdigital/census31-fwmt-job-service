server:
  port: 8025

spring:
  application:
    name: census31-fwmt-job-service
  main:
    allow-circular-references: true
  security:
    user:
      name: "user"
      password: "password"
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=fwmtg
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yml
    default-schema: fwmtg
    liquibase-schema: fwmtg
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=fwmtg
    user: postgres
    password: postgres
  cloud:
    gcp:
      project-id: ${FWMT_PUBSUB_PROJECT:fwmt-local}
      credentials:
        enabled: false
      firestore:
        enabled: false
      pubsub:
        enabled: true
        emulator-host: ${PUBSUB_EMULATOR_HOST:localhost:8085}

management:
  # Place /info and /health in the root
  endpoints.web.base-path: ''
  endpoints:
    web:
      exposure:
        include: ${MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE:health,info,env,refresh}
  endpoint:
    health:
      show-details: always
    env:
      enabled: ${APP_TESTING:false}
      post:
        enabled: ${APP_TESTING:false}
    refresh:
      enabled: ${APP_TESTING:false}

logging:
  profile: DEV
  level:
    root: WARN
    uk.gov.ons.census.fwmt: INFO
    org.springframework: INFO
    org.springframework.security: INFO
    org.springframework.web: INFO
    org.hibernate: INFO
    org.apache.commons.dbcp2: INFO
    liquibase: DEBUG


totalmobile:
  username: "user"
  password: "password"
  baseUrl: "http://localhost:8000/"
  healthcheckPath: "swagger/index.html"
  comet:
    clientID: ""
    clientSecret: ""
    resource: "https://int-ons-comet-api-app-ukwest.azurewebsites.net"
    authority: "https://login.microsoftonline.com/05057611-67c0-4390-85ef-2c623ff4104f/oauth2/v2.0/token"
  operation:
    case:
      create:
        path: "cases/"
rmapi:
  username: "user"
  password: "password"
  baseUrl: "http://localhost:8000/"

decryption:
  # Private key is not bundled in the repo (see docs/gitguardian-pgp-private-key.md).
  # Override via DECRYPTION_PGP / DECRYPTION_PASSWORD or Spring config in each environment.
  pgp: "${DECRYPTION_PGP:file://${user.home}/.fwmt/keys/decryption.private}"
  password: "${DECRYPTION_PASSWORD:}"
 


fwmt:
  workTypes:
    ccs:
      duration: 5
    ce:
      duration: 5
    hh:
      duration: 5
    ohs:
      duration: 5

eq:
  url: "https://fieldservice-ENV_NAME.census-gcp.onsdigital.uk/launch/"

spg:
  followUpDate: 11111

feature-flags:
  hh:
    create: ${FEATURE_HH_CREATE:false}
    update: ${FEATURE_HH_UPDATE:false}
    cancel: ${FEATURE_HH_CANCEL:false}
    pause: ${FEATURE_HH_PAUSE:false}
    reactivate: ${FEATURE_HH_REACTIVATE:false}
  ce:
    create: ${FEATURE_CE_CREATE:false}
    update: ${FEATURE_CE_UPDATE:false}
    cancel: ${FEATURE_CE_CANCEL:false}
    pause: ${FEATURE_CE_PAUSE:false}
    reactivate: ${FEATURE_CE_REACTIVATE:false}
    switch_ce_type: ${FEATURE_CE_SWITCH_CE_TYPE:false}
  spg:
    create: ${FEATURE_SPG_CREATE:false}
    update: ${FEATURE_SPG_UPDATE:false}
    cancel: ${FEATURE_SPG_CANCEL:false}
    pause: ${FEATURE_SPG_PAUSE:false}
    reactivate: ${FEATURE_SPG_REACTIVATE:false}
  ccs:
    create: ${FEATURE_CCS_CREATE:false}
    update: ${FEATURE_CCS_UPDATE:false}
    cancel: ${FEATURE_CCS_CANCEL:false}
    pause: ${FEATURE_CCS_PAUSE:false}
    reactivate: ${FEATURE_CCS_REACTIVATE:false}
  nc:
    create: ${FEATURE_NC_CREATE:false}
    update: ${FEATURE_NC_UPDATE:false}
    cancel: ${FEATURE_NC_CANCEL:false}
    pause: ${FEATURE_NC_PAUSE:false}
    reactivate: ${FEATURE_NC_REACTIVATE:false}
  feedback:
    cancel: ${FEATURE_FEEDBACK_CANCEL:false}

ce:
  followUpDate: "01/05/2020"
  startDate: "01/05/2020"

jobservice:
  transitionRules:
    path: classpath:/transitionRules.txt

resthttp:
  connection:
    timeout: 2000
    requestTimeout: 1000
    socketTimeout: 5000
    pool:
      maxPerRoute: 20
      maxTotal: 100

app:
  testing: false
  messaging:
    provider: pubsub
    maxRetryCount: 5
    destinations:
      rmField: "RM.Field"
      gwField: "GW.Field"
      gwTransientError: "GW.Transient.ErrorQ"
      gwPermanentError: "GW.Permanent.ErrorQ"
    subscriptions:
      gwTransientError: job-service-GW-Transient-ErrorQ
    pubsub:
      rm-field-subscription: job-service-RM-Field
      gw-field-subscription: job-service-GW-Field
